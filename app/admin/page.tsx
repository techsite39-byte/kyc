import Link from "next/link";
import { Activity, ArrowUpRight, CheckCircle2, Clock3, Users } from "lucide-react";
import { AppShell } from "@/components/shell";
import { Badge, Button } from "@/components/ui";
import { getKycProviderMode } from "@/lib/kyc/provider";
export default function Admin() { const mode = getKycProviderMode(); return <AppShell admin><div className="mx-auto max-w-7xl px-5 py-8"><div className="flex items-center justify-between"><div><p className="text-sm font-bold text-brand-600">OPERATIONS</p><h1 className="mt-1 text-3xl font-bold">Admin dashboard</h1><p className="mt-2 text-sm text-slate-500">{mode === "sumsub" ? "SUMSUB SANDBOX · TEST ENVIRONMENT" : "DEMO MODE · Mock KYC provider"}</p></div><Link href="/admin/settings"><Button variant="secondary">Configure provider</Button></Link></div><div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><Metric icon={<Activity />} label="Checks today" value="248" note="+12.5%" /><Metric icon={<CheckCircle2 />} label="Approval rate" value="91.4%" note="+3.2%" /><Metric icon={<Clock3 />} label="Avg. completion" value="2m 48s" note="-18s" /><Metric icon={<Users />} label="Active users" value="1,842" note="+8.1%" /></div><div className="mt-8 grid gap-6 lg:grid-cols-5"><div className="rounded-2xl border border-slate-200 bg-white p-6 lg:col-span-3"><div className="flex justify-between"><h2 className="font-bold">Verification volume</h2><span className="text-xs text-slate-400">Last 7 days</span></div><div className="mt-8 flex h-48 items-end gap-3">{[42, 61, 52, 74, 68, 92, 80].map((n, i) => <div className="flex flex-1 flex-col items-center gap-2" key={i}><div className="w-full rounded-t-lg bg-brand-500" style={{ height: `${n}%` }} /><span className="text-xs text-slate-400">{["M", "T", "W", "T", "F", "S", "S"][i]}</span></div>)}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-6 lg:col-span-2"><h2 className="font-bold">Recent checks</h2><div className="mt-4 space-y-4">{["maya.chen@example.com", "jamal.smith@example.com", "sofia.rossi@example.com"].map((email, i) => <div className="flex items-center gap-3" key={email}><div className="grid h-9 w-9 place-items-center rounded-full bg-brand-50 text-xs font-bold text-brand-700">{email[0].toUpperCase()}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{email}</p><p className="text-xs text-slate-400">Passport · 2m ago</p></div><Badge tone={i === 1 ? "amber" : "green"}>{i === 1 ? "Review" : "Approved"}</Badge></div>)}</div><Link className="mt-5 flex items-center text-sm font-semibold text-brand-600" href="/admin/verifications">View all <ArrowUpRight className="ml-1 h-4 w-4" /></Link></div></div></div></AppShell> }
function Metric({ icon, label, value, note }: { icon: React.ReactNode; label: string; value: string; note: string }) { return <div className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex justify-between text-brand-600">{icon}<span className="text-xs text-emerald-600">{note}</span></div><p className="mt-5 text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div> }
