import { LockKeyhole, ShieldCheck, EyeOff } from "lucide-react";
import { PublicShell } from "@/components/shell";
export default function Security() { return <PublicShell><main className="mx-auto max-w-5xl px-5 py-16"><p className="text-sm font-bold uppercase tracking-widest text-brand-600">Trust center</p><h1 className="mt-3 text-4xl font-bold">Security is a product feature.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-slate-600">Explore the principles behind a responsible verification workflow. This is a demo, so no real identity checks are performed.</p><div className="mt-12 grid gap-5 md:grid-cols-3"><Card icon={<LockKeyhole />} title="Encrypted by default" text="Production implementations should encrypt data in transit and at rest." /><Card icon={<EyeOff />} title="Minimal retention" text="This demo never permanently stores uploaded documents or selfies." /><Card icon={<ShieldCheck />} title="Transparent outcomes" text="Every result clearly identifies whether it came from demo mode." /></div></main></PublicShell> }
function Card({ icon, title, text }: { icon: React.ReactNode; title: string; text: string }) { return <div className="rounded-2xl border border-slate-200 bg-white p-6"><div className="mb-5 text-brand-600">{icon}</div><h2 className="font-bold">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{text}</p></div> }
