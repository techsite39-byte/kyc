import { PublicShell } from "@/components/shell";
export default function Privacy() { return <PublicShell><Legal title="Privacy policy" intro="This demo uses only temporary, local data. Uploaded identity documents and selfies are not permanently stored." sections={["Information we collect", "How we use information", "Retention and deletion", "Your rights"]} /></PublicShell> }
function Legal({ title, intro, sections }: { title: string; intro: string; sections: string[] }) { return <main className="mx-auto max-w-3xl px-5 py-16"><p className="text-sm font-bold uppercase tracking-widest text-brand-600">KeystoneID</p><h1 className="mt-3 text-4xl font-bold">{title}</h1><p className="mt-5 rounded-2xl bg-brand-50 p-5 leading-7 text-slate-700">{intro}</p>{sections.map((s) => <section className="mt-10" key={s}><h2 className="text-xl font-bold">{s}</h2><p className="mt-3 leading-7 text-slate-600">KeystoneID is a demonstration product. In a production deployment, this section would describe the precise controls, purposes, legal bases, retention periods, and user choices applicable to the service.</p></section>)}</main> }
